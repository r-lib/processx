# Base64 Encoding and Decoding

Base64 Encoding and Decoding

## Usage

``` r
base64_decode(x)

base64_encode(x)
```

## Arguments

- x:

  Raw vector to encode / decode.

## Value

Raw vector, result of the encoding / decoding.
