
OBJECTS = init.o unix/childlist.o unix/connection.o unix/poll.o \
          unix/processx.o unix/sigchld.o unix/utils.o           \
	  unix/interruptible-calls.o

all: clean

clean:
	rm -f $(SHLIB) $(OBJECTS)
